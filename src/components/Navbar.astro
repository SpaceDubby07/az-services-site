<script>
  let scrollPosition = 0;

  function updateScrollPosition() {
    scrollPosition = window.scrollY;
    console.log(scrollPosition);
    const header = document.querySelector('header');
    if (header) {
      if (scrollPosition > 800) {
        header.classList.add('hidden');
        header.classList.remove('backdrop-blur-md'); // Optional: Remove the blur if hidden
      } else if (scrollPosition > 100) {
        header.classList.add('backdrop-blur-md');
        header.classList.remove('hidden'); // Optional: Ensure header is visible
      } else {
        header.classList.remove('backdrop-blur-md', 'hidden');
      }
    }
  }

  if (typeof window !== 'undefined') {
    window.addEventListener('scroll', updateScrollPosition);
  }
</script>

<header class="fixed w-full z-10">
  <nav class="flex items-center justify-between p-4">
    <div class="text-white text-2xl font-bold">
      <a href="/">Logo</a>
    </div>
    <div class="space-x-2">
      <a
        href="/"
        class="text-white hover:text-gray-200 transition duration-300"
        >Home</a
      >
      <a
        href="/services"
        class="text-white hover:text-gray-200 transition duration-300"
        >Services</a
      >
      <a
        href="/contact"
        class="text-white hover:text-gray-200 transition duration-300"
        >Contact</a
      >
    </div>
  </nav>
</header>
